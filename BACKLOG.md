# Project Backlog

Full backlog across SUT (`clinic-booking-api`) + test repo (`clinic-booking-api-tests`).

**Project 3 — Roguelike engine design:** see [`docs/ROGUELIKE.md`](docs/ROGUELIKE.md)

Legend: `[x]` done · `[ ]` pending · `[~]` skipped/decided against

---

## ⏳ Pending verification — requires Docker

| # | What to run | What to verify |
|---|-------------|----------------|
| 1 | `cd sut && docker compose -f docker-compose.kafka.yml up -d` then `KAFKA_BROKER=localhost:9092 npm run dev` | Log shows `"event":"kafka.connected"` on startup |
| 2 | Book an appointment via API or UI | `docker exec -it sut-kafka-1 kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic clinic.appointment.booked --from-beginning --max-messages 1` returns JSON with `appointmentId`, `patientId`, `timestamp`, `requestId` |
| 3 | `npm run dev` without `KAFKA_BROKER` | SUT starts cleanly; log shows warn `"kafka.connect_failed"` (not error); all API calls work normally |

---

## ⏳ Pending verification — requires Anthropic API credits

All three items were implemented and tested locally; the infrastructure works but the actual AI call failed with "credit balance too low". Verify in this order after top-up.

| # | What to run | What to verify |
|---|-------------|----------------|
| 1 | `DEMO_BUG_REPORTER=true ANTHROPIC_API_KEY=$(grep ANTHROPIC_API_KEY .env \| cut -d= -f2) npx playwright test bug-reporter.demo` | Allure report (`npm run report`) shows two failed tests, each with a "Bug Report" attachment containing AI-generated markdown (title, severity, steps to reproduce, actual vs expected). File also saved to `bug-reports/`. |
| 2 | `ANTHROPIC_API_KEY=$(grep ANTHROPIC_API_KEY .env \| cut -d= -f2) npm run ai:gap-analysis` | `docs/AI_GAP_ANALYSIS.md` regenerated by Claude Haiku; compare with current manual version; commit if output is better. |
| 3 | `npx playwright test --grep @rag` | All `@rag` tests pass (LLM eval, LLM judge, RAG completeness, prompt injection, graceful degradation). These use real Claude calls and were last green before the balance ran out. |

---

## SUT Features

| Status | Feature | Notes |
|--------|---------|-------|
| [x] | Appointments lifecycle + state machine | pending → confirmed → completed; reject/cancel |
| [x] | JWT auth (register / login / refresh) | |
| [x] | Waitlist — join/leave/list + auto-promotion | |
| [x] | Waitlist offers (Variant B) | patient gets offer when slot frees up |
| [x] | Webhook notifications | `notificationService.js`; `notifications.webhook.test.ts` (3 tests) |
| [x] | WebSocket notifications (doctor) | real-time toast + auto-refresh; SUT bug fixed 2026-05-03 |
| [x] | Payments / online consultations | `PAYMENT_MODE=disabled\|mock_success\|mock_fail`; `X-Idempotency-Key` |
| [x] | AI recommendation — RAG | real Claude call (`claude-haiku-4-5-20251001`); mock mode for CI |
| [x] | Reschedule appointment | `PATCH /appointments/:id/reschedule`; SUT done 2026-05-09 |
| [x] | Recurring appointments | `POST /appointments/recurring` + series cancel; SUT done 2026-05-15 |
| [x] | COMPLETED status | `confirmed → completed`; doctor action; done 2026-05-15 |
| [x] | Pagination | `GET /appointments/my` + `/doctor`; `?page=&limit=`; `{ data, total, page, limit, totalPages }` |
| [x] | Kafka producer | 8 topics; events on all state changes; graceful degradation if no broker; done 2026-05-16 |
| [x] | Chaos mode | `CHAOS_ENABLED`; deterministic RNG via `CHAOS_SEED` |
| [x] | Rate limiting | login / register / booking / AI endpoint |
| [x] | Structured logging (Pino) | `requestId` on every request |
| [x] | Metrics endpoint | booking/cancel/confirm/completed counters |
| [x] | Docker + docker-compose.test.yml | CI-ready; healthcheck; local == CI; done 2026-05-09 |
| [x] | Observability stack | Loki + Promtail + Grafana via `docker-compose.observability.yml` |
| [x] | OpenAPI / Swagger UI | `/api/docs`; contract drift guard in tests |
| [~] | Audit trail (DB table) | skipped — Pino + Loki covers at production level |
| [~] | Redis caching (doctors list) | skipped 2026-05-16 — no new test patterns for QA portfolio |
| [~] | Admin dashboard endpoint | skipped 2026-05-16 — no meaningful test scenarios |
| [x] done 2026-08-21 | **AI service extraction + Pact redesign** | Запись стояла выполненной с 2026-05-18, но проводки не было: `callAiService()` не существовало ни в одной ветке, `AI_SERVICE_UNAVAILABLE` отсутствовал в коде SUT, compose сервис не поднимал, а тест на недоступность скипался всегда. Реально сделано 2026-08-21: `src/services/aiServiceClient.js` + `AI_SERVICE_URL`/`AI_SERVICE_TIMEOUT_MS`/`AI_SERVICE_DEGRADE`; `AI_SERVICE_UNAVAILABLE` (503) в контракте, отдельно от `CLAUDE_UNAVAILABLE`; сервис в `docker-compose.yml` + overlay `docker-compose.ai-service.yml`; 28 unit-тестов (клиент + parity двух реализаций retrieval, которые успели разойтись и вернули бы B-05); structured outputs в `ai-service` вместо ручного JSON.parse — без этого сервис падал на каждом реальном вызове; джоба `Delegated AI service` в `api-tests.yml` |
| [x] done 2026-08-22 | **Провайдерская верификация мобильного контракта** | Было: `clinic-mobile-tests/pacts/clinic-mobile-clinic-booking-api.json` генерировался consumer-тестом, коммитился с подписью «checked in for provider verification» — и не читался ничем: имя файла встречалось только в двух `.md`. Мобильная CI-джоба была зелёная, потому что consumer-тест проверяет свой же мок; граница mobile→API (MOB-01, `token` vs `accessToken`) со стороны провайдера оставалась незащищённой. Стало: контракт скопирован в `pacts/`, `tests/api/pact/mobile.pact.provider.test.ts` верифицирует все 6 интеракций против живого SUT. Состояние под литеральные id контракта (провайдерских стейтов consumer не писал) подставляется через `requestFilter`: живой токен, реальный doctorId, свободный слот, своя запись на отмену — подменяются только идентификаторы, не имена полей и не статусы. Почему так: без брокера провайдер получает pact копией; копия сверяется с оригиналом из `MOBILE_TESTS_ROOT` в самом тесте и падает при расхождении, поэтому протухнуть незаметно не может. Результат первого прогона: 5/6 зелёных, 6-я нашла B-15 — `isAvailable` приходит `1`, контракт фиксирует boolean; правка SUT отдельным решением |
| [x] done 2026-05-18 | **Kafka full integration** | `tests/api/appointments.kafka.test.ts` — 9 тестов @kafka: booked/cancelled/confirmed/rejected/rescheduled/completed/recurring_booked/series_cancelled + graceful degradation; `utils/kafkaTestConsumer.ts`; skip guard если `KAFKA_BROKER` не задан |
| [x] | **Multi-step booking wizard** | 4-step URL wizard; 7 UI tests + 1 e2e happy path + conflict race; existing tests updated to wizard flow |
| [~] | **WS for patient** | skipped 2026-05-19 — decided against |
| [x] done 2026-05-19 | **Optimistic UI on cancel** | `btn.disabled` + `li.style.opacity=0.35` до API call; rollback на catch; guard `if (btn.disabled) return` в делегированном хендлере; `data-qa="patient-appt-item"` + `data-appt-id` на `li`; 3 UI теста × 2 браузера = 6/6 зелёных: mid-flight state, rollback на 500, double-click blocked |
| [~] | **Slot hold / provisional booking** | skipped 2026-05-18 — не будем делать |
| [~] | **Optimistic locking** | skipped 2026-05-19 — concurrency already covered; marginal new pattern |
| [x] done 2026-05-18 | **Appointment notes** | `POST/GET /appointments/:id/notes`; B-08 (XSS), B-09 (IDOR), B-10 (state machine) — всё исправлено в SUT, 9 тестов зелёные |
| [x] done 2026-05-18 | **Doctor ratings** | `POST /appointments/:id/rate` + `GET /doctors/:id/rating`; B-11 (state machine), B-12 (idempotency), B-13 (data leak) — всё исправлено, 9 тестов зелёные |
| [x] done 2026-05-19 | **Search / filtering** | `GET /appointments/my?status=&doctorId=&from=&to=`; динамический WHERE + INNER JOIN slots в репозитории; parseFilterParams() в роуте — 400 на bogus status/doctorId/date; 11 тестов: статус/doctorId/from/to/combined/validation; all green |
| [x] done 2026-05-18 | **Soft delete** | `deletedAt` на users уже в схеме; `DELETE /auth/me`; JWT/refresh revoked after delete; EMAIL_RETIRED; DB integrity check; 8 тестов зелёные |
| [ ] | **HTTP Idempotency Keys** | `Idempotency-Key: uuid-v4` заголовок на `POST /appointments`; таблица `idempotency_keys(key, response_body, status_code, created_at)`; middleware: ключ есть → вернуть кеш; ключ нет → обработать + сохранить; TTL 24ч; error `IDEMPOTENCY_KEY_CONFLICT` если тот же ключ + другое тело; тест-сценарии: (1) retry → тот же ID, нет дубля в БД; (2) тот же ключ + другое тело → 422; (3) истёкший ключ → обрабатывается как новый; курсовой differentiator — idempotency key testing нигде не преподают практически; источник: habr.com/ru/companies/yandex/articles/442762 |
| [~] | **Admin role + privilege escalation** | skipped 2026-05-19 — decided against |
| [~] | **Bulk cancel (doctor sick day)** | admin only; 🟡 partial transaction, waitlist cascade |
| [~] | **GDPR data export** | `GET /patients/me/export`; 🟡 IDOR risk |
| [x] done 2026-05-19 | **Appointment type / duration** | `type` column на appointments (DEFAULT 'consultation'); `POST /appointments` принимает `type: "consultation"|"procedure"`; 400 на invalid type; B-14 planted: SLOT_TOO_SHORT check отсутствует → procedure в 15min слот проходит; 6 тестов (3 @smoke + 400 + 401 + B-14 test.fail()) |
| [x] done 2026-05-16 | **Doctor schedule / working hours** | `PUT /me/schedule` + `GET /:id/schedule`; валидация в slot creation; `OUTSIDE_WORKING_HOURS` 422; UTC comparison; backward compat; UI в `doctor-schedule.html`; тесты: API 10 + UI 5 + E2E 1 (DB check) |

---

## Detailed feature specs

### ~~Reschedule appointment~~ ✅ done 2026-05-16
`PATCH /appointments/:id/reschedule` — SUT + UI + тесты полностью готовы. `appointments.reschedule.test.ts` написан 2026-05-16, 8/8 тестов зелёные. Нашёл и исправил SUT баг B-07 (неправильный порядок операций в транзакции — тест с waitlist-пациентом поймал, 7 тестов без waitlist пропустили).

**State machine:** `pending` / `confirmed` → reschedule → always `pending` (doctor must re-confirm). Atomic: free old slot + promote waitlist + book new slot.

**Error codes:** `SLOT_TAKEN` (409), `DOCTOR_MISMATCH` (422), `SAME_SLOT` (422), `INVALID_TRANSITION` (422), `SLOT_NOT_FOUND` (404).

**Test plan:**
- 200 reschedule from `pending` → new slot, status `pending`
- 200 reschedule from `confirmed` → status resets to `pending`
- 409 new slot already taken
- 422 reschedule `cancelled` appointment
- 422 new slot belongs to different doctor
- 422 same slot as current
- 403 patient does not own appointment
- waitlist cascade: old slot freed → waitlist patient promoted
- UI: Reschedule button visible for pending/confirmed only
- UI: successful reschedule refreshes list

---

### ~~COMPLETED appointment status~~ ✅ done 2026-05-15
`PATCH /appointments/:id/complete` — doctor-only terminal transition from `confirmed`.

**Key design:** slot NOT freed (visit occurred); waitlist NOT promoted; patient notified via WS + webhook.

**Test plan — Tier 1:**
- `confirmed → completed` → 200, `status: "completed"`
- Slot stays unavailable after complete (guards copy-paste of cancel logic)
- Waitlist NOT promoted after complete (guards accidental `promoteFromWaitlist` call)
- `pending → completed` → 422 `INVALID_TRANSITION`
- `completed → completed` → 422 (double-click)
- Wrong doctor → 403 (IDOR)
- Patient calls `/complete` → 403 (RBAC)

**Test plan — Tier 2:**
- WS: patient receives `appointment.completed` event
- E2E: completed appointment shows no action buttons in patient UI

---

### Recurring appointments ✅ Done 2026-05-17

SUT done 2026-05-15. `POST /api/v1/appointments/recurring` + `PATCH /api/v1/appointments/series/:seriesId/cancel`.

**Design:** `seriesId` (UUID) on each appointment; weekly pattern; partial booking model.

**API — `tests/api/appointments.recurring.test.ts` (9 tests, all green):**
- `POST /recurring` — 201 books 2 weekly slots, returns seriesId @smoke
- `POST /recurring` — 201 partial: one slot unavailable → bookedCount=1, unavailable[0].slotId correct
- `POST /recurring` — 404 SLOT_NOT_FOUND for non-existent startSlotId
- `POST /recurring` — 400 VALIDATION_ERROR for unsupported slotPattern ("daily")
- `POST /recurring` — 400 count below minimum (count=1)
- `POST /recurring` — 400 count exceeds maximum (count=13)
- `POST /recurring` — 401 without authentication
- `PATCH /series/:seriesId/cancel` — 200 cancels series, frees all slots @smoke
- `PATCH /series/:seriesId/cancel` — 404 SERIES_NOT_FOUND for unknown seriesId

**Finding:** `INVALID_PATTERN` in repository layer is dead code — route validates `slotPattern` first and returns `VALIDATION_ERROR`. Recorded as DEAD-01 in KNOWN_ISSUES.md.

**UI — `tests/ui/appointments.recurring.ui.test.ts` (4 tests × 2 browsers = 8, all green):**
- series tag visible when seriesId present, absent without
- cancel-series button shown only for pending/confirmed with seriesId
- cancel series via UI: confirm dialog → list reloads → toast "Series cancelled."
- API error shows inline notice "Couldn't cancel the series"

**Finding during UI test:** default sort on page is `id-desc` ("Newest first") — test must account for DOM order ≠ API response order. Recorded as SORT-01.

**E2E — `tests/e2e/appointments.recurring.e2e.test.ts` (2 tests, all green):**
- book series via API → both appointments visible in UI with series tag + DB seriesId verified @e2e @smoke
- cancel series via UI → API status + DB status both "cancelled" @e2e @smoke

---

### Kafka event tests
`tests/api/appointments.kafka.test.ts` — skip guard: `KAFKA_BROKER` not set → all tests skipped.
Requires: `docker compose -f sut/docker-compose.kafka.yml up -d`.

**Test plan (@kafka):**
- `POST /appointments` → event on `clinic.appointment.booked`; payload: `appointmentId`, `patientId`, `slotId`, `status: "pending"`, `timestamp`, `requestId` @smoke
- `PATCH /:id/cancel` (patient) → event on `clinic.appointment.cancelled`; `cancelledBy: "patient"`
- `PATCH /:id/confirm` (doctor) → event on `clinic.appointment.confirmed`; `doctorId`
- `PATCH /:id/reject` (doctor) → event on `clinic.appointment.rejected`; `doctorId`
- `PATCH /:id/reschedule` → event on `clinic.appointment.rescheduled`; `newSlotId`
- `PATCH /:id/complete` → event on `clinic.appointment.completed`; `doctorId`, `status: "completed"`
- `POST /recurring` → event on `clinic.appointment.recurring_booked`; `seriesId`, `bookedCount`, `appointmentIds[]`
- `PATCH /series/:seriesId/cancel` → event on `clinic.appointment.series_cancelled`; `seriesId`, `cancelledCount`
- Graceful degradation: `KAFKA_BROKER` not set → SUT starts, API calls succeed, no errors @smoke

---

### Pagination — `GET /appointments/my` + `GET /appointments/doctor`
SUT done 2026-05-15. `?page=&limit=` (page ≥ 1, limit 1–100); response `{ data, total, page, limit, totalPages }`.

**API test plan — `tests/api/appointments.pagination.test.ts` (@api):**
- `GET /appointments/my` no params → 200 `{ data[], total, page:1, limit:20, totalPages }` shape valid @smoke
- `GET /appointments/my?page=1&limit=5` → returns max 5 items; `totalPages = ceil(total/5)`
- `GET /appointments/my?page=2&limit=1` → returns second item (correct offset); off-by-one in `OFFSET=(page-1)*limit` shows wrong appointments
- `GET /appointments/my?limit=0` → 400 `VALIDATION_ERROR` (division by zero → `totalPages: NaN/Infinity`)
- `GET /appointments/my?page=0` → 400 `VALIDATION_ERROR` (negative OFFSET)
- `GET /appointments/my?page=abc` → 400 `VALIDATION_ERROR` (NaN in OFFSET)
- `GET /appointments/doctor` no params → 200 paginated response (doctor JWT) @smoke

---

---

## Tests — pending implementation

| Status | Test file / item | Notes |
|--------|-----------------|-------|
| [x] done 2026-05-16 | `appointments.reschedule.test.ts` | 8 API тестов: все зелёные; нашёл и исправил SUT баг B-07 (неправильный порядок операций в транзакции при наличии waitlist) |
| [x] done 2026-05-16 | `reschedule.ui.test.ts` | 2 UI теста: кнопка видна для pending/confirmed, скрыта для cancelled/completed; после reschedule список обновляется → pending badge + toast; 4/4 (chromium + mobile-chrome) |
| [x] done 2026-05-17 | `appointments.recurring.test.ts` | 9 API + 4 UI (recurring.ui) + 2 E2E (recurring.e2e); все зелёные; DEAD-01 найден |
| [x] done 2026-05-18 | `appointments.kafka.test.ts` | 9 тестов @kafka: booked/cancelled/confirmed/rejected/rescheduled/completed/recurring_booked/series_cancelled + graceful degradation; `utils/kafkaTestConsumer.ts`; skip guard если `KAFKA_BROKER` не задан |
| [x] done 2026-05-16 | `appointments.pagination.test.ts` | 7 тестов: default shape @smoke, limit=5, page=2&limit=1 off-by-one, limit=0/page=0/page=NaN → 400, doctor endpoint @smoke |
| [x] done 2026-05-16 | **List-level AJV schemas** | `validateAppointmentList` + `validatePaginatedAppointments` добавлены в `data/schemas/appointmentSchemas.ts` |
| [x] closed 2026-08-21 | Regression: "chest pain" → Cardiologist | B-05 fixed in SUT (`fcccd6d`, multi-word keyword matching). The `test.fail()` marker alerted on the fix and was removed; `unit/ai.retrieval.test.ts` now guards the corrected behaviour as an ordinary regression test |
| [x] done 2026-05-16 | Regression: `doctors.length > 0` | добавлен assert в `api/ai.recommend.test.ts`; регрессия B-06; использует "chest pain and shortness of breath" → Cardiologist (seeded) |
| [x] done 2026-05-18 | AI model drift (scheduled) | `model-drift.yml`; cron Monday 09:00 UTC + manual; запускает `@rag` с реальным ключом; артефакты 90 дней |
| [x] done 2026-05-18 | AI-генерация тестовых данных | `utils/aiTestDataGenerator.ts` + `tests/api/content.stress.test.ts`; 10 статических + 1 `@ai-data`; graceful degradation без ключа |
| [x] done 2026-05-18 | AI-generated CI run summary | `scripts/ai-ci-summary.js`; читает `test-results.json` → Claude → `ci-summary/summary.md`; шаги в `api-tests.yml` |
| [~] | AI scenario generation | skipped 2026-05-19 — закрыто `ai-gap-analysis.js` §4 (Additional test scenarios); для курса использовать gap analysis как живое демо вместо отдельного скрипта |

---

## Testing backlog — completed

| Item | Status | Notes |
|------|--------|-------|
| Mobile viewport | [x] done 2026-05-01 | `devices["Pixel 7"]`; 12/12 pass |
| Chaos mode | [x] done 2026-05-02 | 6 tests complete |
| Visual regression | [x] done 2026-05-15 | `visual.test.ts`; badge colours + login/register baselines |
| Rate limit tests — CI | [x] done 2026-05-15 | `rate-limit.yml` workflow; manual trigger |
| Mutation testing | [x] done 2026-05-01 | 92% score; `appointmentStateMachine.js` |
| Load testing (k6) | [x] done 2026-05-01 | `k6/booking-flow.js`; 50 VUs |
| k6 in CI with performance gate | [x] done 2026-05-12 | `performance.yml`; exits 1 on threshold breach |
| Swagger / contract drift guard | [x] done 2026-05-04, extended 2026-06-07 | `contract.drift.test.ts`; 9 tests — 5 OpenAPI spec + 4 live shape assertions (login, doctors, create appointment, list appointments) |
| TypeScript migration | [x] done 2026-05-15 | all 75 files migrated |
| LLM as a judge | [x] done 2026-05-04 | second Claude evaluates reasoning vs symptoms; `@rag` |
| RAG completeness metrics | [x] done 2026-05-09 | coverage ≥ 50%; `@rag` |
| Contract testing with Pact | [x] done 2026-05-08 | consumer + provider; currently monolith |
| DB state assertions | [x] done 2026-05-01 | inline via `utils/dbClient.ts` |
| Observability-driven testing | [x] done 2026-05-02 | Loki query after booking |
| Property-based testing (fast-check) | [x] done 2026-05-02 | state machine invariants |
| Concurrency tests | [x] done | double-cancel + concurrent waitlist promotion |
| Security tests (IDOR + JWT) | [x] done 2026-04-30 | caught real unplanted IDOR |
| Accessibility tests (axe-core) | [x] done 2026-04-30 | 3 real a11y issues fixed |
| BrowserStack integration | [x] done 2026-05-12 | 22/22 on OSX Sequoia + Windows 11; **не включать в курс** — tool configuration, не technique; safari finding → content material |
| Schemathesis API fuzzing | [x] done 2026-05-12 | 408+ scenarios; 3 real gaps found |
| OWASP ZAP in CI | [x] done | `.github/workflows/security-scan.yml` — ZAP baseline против поднятого SUT, отчёт в артефактах; документация в TEST_STRATEGY.md §15.9. (Строка до 2026-08-21 утверждала, что workflow не существует — он к тому моменту уже был.) |
| API error states (page.route()) | [x] done 2026-05-15 | `api-error-states.test.ts`; 3 scenarios |
| AI test generation artifact | [x] done 2026-05-04 | `docs/AI_TEST_GENERATION.md` |
| Test orthogonality map | [x] done 2026-05-02 | §17 in TEST_STRATEGY.md; 36 files mapped |

---

## UI improvements

| Status | Signal | Item |
|--------|--------|------|
| [x] done 2026-05-15 | 🔴 | **Toast notifications** — `ClinicApp.showToast()`; replaced `#bannerSuccess`; E4 test updated |
| [x] done 2026-05-15 | 🔴 | **Coloured status badges** — `.status-badge` CSS; `data-qa="status-badge"` |
| [x] done 2026-05-15 | 🔴 | **Dark mode** — `@media (prefers-color-scheme: dark)`; all workspaces |
| [x] done 2026-05-15 | 🟡 | **Empty state** — SVG icon + title + CTA; doctor + patient pages |
| [x] done 2026-05-16 | 🟡 | **Pagination controls** — Prev/Next + «Page N of M» + Per page select в `patient-appointments.html` и `doctor-appointments.html`; скрыты при `totalPages ≤ 1`; UI тесты: `patient-appointments.pagination.test.ts` + `doctor-appointments.pagination.test.ts`; 10/10 тестов × 2 браузера = 20 зелёных |
---

## Portfolio differentiators — advanced tooling

| Status | Signal | Item | Notes |
|--------|--------|------|-------|
| [x] done 2026-05-09 | 🔴 сильный | AI-generated bug report from failed test | `utils/aiBugReporter.ts`; Allure attachment; `bug-reports/` |
| [x] done 2026-05-09 | 🟡 средний | AI gap analysis from OpenAPI spec | `scripts/ai-gap-analysis.js`; `docs/AI_GAP_ANALYSIS.md` |
| [x] done 2026-05-18 | 🔴 сильный | **AI service extraction + Pact redesign** | → см. SUT Features выше |
| [x] done 2026-05-18 | 🔴 сильный | **Impact Analysis (Claude + git diff)** | `scripts/impact-analysis.js` + `.github/workflows/impact-analysis.yml`; PR trigger; git diff → Claude → минимальный набор тест-файлов → ai-ci-summary → PR comment; fallback без ключа = all tests |
| [x] done 2026-05-18 | 🟡 средний | **UI states testing** | error ✅ (`api-error-states.test.ts`); success + empty + filter ✅ (`ui-states.test.ts`, 9 тестов: patient empty/filter/cancel-toast, booking empty/success/disabled, doctor empty/error); disabled ✅ (`ui-disabled-states.test.ts`, 2 теста: doctor select + offer accept re-enable); loading — нет отдельных skeleton/spinner в SUT |
| [x] done 2026-05-18 | 🔴 сильный | **AI model drift (scheduled CI)** | `model-drift.yml`; cron Monday 09:00 UTC + manual; запускает `@rag` с реальным ключом; артефакты 90 дней |
| [x] done 2026-05-18 | 🔴 сильный | **AI-генерация тестовых данных** | `utils/aiTestDataGenerator.ts` + `tests/api/content.stress.test.ts`; 10 статических + 1 `@ai-data`; graceful degradation без ключа |
| [x] done 2026-05-18 | 🔴 сильный | **AI-generated CI run summary** | `scripts/ai-ci-summary.js`; читает `test-results.json` → Claude → `ci-summary/summary.md`; шаги в `api-tests.yml` |
| [x] done 2026-05-23 | 🔴 сильный | **Metamorphic testing для AI-эндпоинта** | Новый describe `metamorphic consistency @api` в `tests/api/ai.recommend.test.ts`: 3 теста — cardiac/skin/pediatric симптомы, 5 формулировок каждый → `Set(specialties).size === 1`. Работает с `AI_MOCK_RESPONSE=true` (тестирует retrieval layer) и с реальным ключом (тестирует LLM consistency). Allure параметры: phrasings count + unique specialties. |
| [x] done 2026-05-23 | 🔴 сильный | **AI атакует AI — adversarial dynamic generation** | Модифицирован prompt-injection тест в `@rag` describe: Claude Haiku генерирует 5 adversarial inputs динамически (вместо статического массива), затем каждый отправляется в SUT. Allure parameter логирует сгенерированные инпуты. Требует реального ANTHROPIC_API_KEY. |
| [x] done 2026-05-23 | 🟡 средний | **Distribution drift detection** | `data/specialty-distribution-baseline.json` — corpus 12 симптомов (2 на специальность), expectedDistribution (каждая ≈0.167), tolerancePerSpecialty: 0.15. Новый describe `distribution drift @rag`: запускает весь corpus, считает реальное распределение, проверяет |actual − expected| ≤ 0.15. Allure attachment с before/after распределением. |
| [x] done 2026-05-24 | 🟡 средний | **AI flakiness classifier** | `scripts/flakiness-classifier.js`; читает `bug-reports/*.md` → группирует по тест-имени → Claude Haiku → классифицирует причину: timing / state-leak / environment / randomness / genuine-bug; markdown отчёт в `flakiness-report/`; `npm run ai:flakiness`; платная AI-секция, Курс 1 |
| [x] done 2026-05-25 | 🟡 средний | **Playwright MCP — discovery vs contract + coverage tracking** | курсовой модуль готов: `course/mcp-lesson.md`; `docs/mcp-demo.md` (реальный output) + `docs/mcp-config.md`; Pact consumer test `tests/api/pact/web.pact.consumer.test.ts`; контраст exploration vs living contract; `scripts/mcp-session-tracker.js` — CDP-based coverage tracker: логирует visitedRoutes + calledEndpoints, warnings при отсутствии POST или <3 routes, JSON report в `coverage/`; `npm run mcp:track` |
| [x] done 2026-05-24 | 🟡 средний | **AI-assisted test generation — живое демо** | `scripts/ai-test-generator.js`; читает `../sut/openapi/openapi.yaml` → Claude Haiku → черновик `.test.ts` по конвенциям проекта (`METHOD /path — STATUS desc @tag`); `npm run ai:test-gen -- --tag Auth`; stub без API key; `docs/AI_TEST_GENERATION.md` — документация; Курс 1 |
| [ ] | 🔴 сильный | **SSE / Streaming response testing** 💰 Wave 2, Курс 1 платно | Добавить streaming endpoint в AI-сервис (`text/event-stream`); Playwright `page.on('response')` → читать partial chunks; assertions: поток завершается корректно, partial content не показывается в UI до финального токена, усечение на 80% ловится, graceful degradation при stream abort; forensic угол: "тест зелёный, но ответ обрезался — пациент видел половину рекомендации" |
| [ ] | 🔴 сильный | **OWASP API Top 10 — mass assignment + SSRF** 💰 Wave 2, Курс 1 платно | Расширить `tests/api/security/` двумя пунктами из OWASP API Top 10 (2023) которых нет: (1) **Mass assignment** (API3) — `PATCH /users` с `{"role":"admin","isAdmin":true}` → assert 400/403, поле проигнорировано; (2) **SSRF** (API7) — endpoint принимает URL как параметр → подать `http://localhost/admin` или `http://169.254.169.254` → assert blocked; дополняет IDOR/BOLA/rate-limits которые уже есть |
| [ ] | 🟡 средний | **Promptfoo / DeepEval — AI eval framework** 💰 Wave 2, Курс 1 платно | Рядом с существующим ручным LLM-as-judge: настроить Promptfoo config (`promptfooconfig.yaml`), прогнать те же симптомы через eval; сравнить: ручной подход vs industry tool; story: "вот как строят AI eval в production командах"; дополняет golden dataset и LLM-as-judge, не заменяет |
| [ ] | 🟡 средний | **Response Groundedness** 💰 Wave 2, Курс 1 платно | Для каждого симптома из golden dataset проверить что рекомендованная специальность семантически связана с симптомом — не только принадлежит `ALLOWED_SPECIALTIES`, но и логична для данного ввода. Реализация: `symptomSpecialtyMap` — допустимое множество специальностей per symptom category (`chest pain → [Cardiologist, Pulmonologist]`, `skin rash → [Dermatologist]`); тест падает если AI вернул валидную специальность но семантически несвязанную с симптомом. Дополняет существующие слои: invariant (результат в допустимом множестве) + statistical (≥8/10 правильных) + groundedness (правильность в контексте ввода). Forensic угол: "invariant прошёл, CI зелёный — но AI рекомендовал Дерматолога при боли в груди" |
| [x] done 2026-05-23 | 🟡 средний | **Расширить skills: selectors, fixtures, common-tasks, typing** | 4 новых `SKILL.md` в `skills/`; selectors — иерархия с обоснованием; fixtures — lifecycle, extend pattern, когда не нужна фикстура; common-tasks — частые операции (DB check, schema assert); typing — strict mode gotchas, `T \| undefined` pattern; CLAUDE.md индекс обновлён |

---

## AI agent infrastructure — idavidov.eu patterns

Источник: серия статей https://idavidov.eu (Book Pattern, AI-Native Workflow, Skills, CLAUDE.md, Explore First, The Scaffold). Паттерны описаны в `course_plan.md` строка 62 (CLAUDE.md+Skills+Hooks раздел, паттерны 1–14) + статьи сайта.

| Status | Item | Описание |
|--------|------|----------|
| [x] done 2026-05-23 | **MUST/SHOULD/WON'T в CLAUDE.md** | Добавлен раздел `## Agent rules — MUST / SHOULD / WON'T` в `tests/CLAUDE.md` и `clinic-mobile-tests/CLAUDE.md`. Web: 7 MUST (TS, клиенты, assertSchema, auth из fixture, tsc), 3 SHOULD (DB assertions, assertSchema, import type), 7 WON'T (new PageClass, any, XPath, waitForTimeout). Mobile: адаптировано под WDIO/factory/Before() паттерн. |
| [x] done 2026-05-23 | **L1 frontmatter в SKILL.md** | YAML frontmatter добавлен во все 9 SKILL.md в `tests/skills/` и все 5 в `clinic-mobile-tests/skills/`. Формат: `name`, `description`, `triggers`. Агент может определить нужный скил без чтения полного файла — достаточно frontmatter. |
| [x] done 2026-05-23 | **Surfacemap для SUT** | **Web:** `tests/docs/SURFACEMAP.md` — все API endpoints с методами/ролями, все UI страницы с URL/data-qa маркерами, data-qa атрибуты по экранам (login/register/booking/patient-appts/doctor-appts/consultations/notifications), per-item data-qa паттерн, error codes по областям, seed accounts. **Mobile:** `clinic-mobile-tests/docs/SURFACEMAP.md` — все 6 экранов с testID таблицами, динамические паттерны (`doctor-appointment-confirm-button-{id}`), формат Android (`//*[@resource-id=...]`) и iOS (`~{testID}`) селекторов, env-флаги. |
| [x] done 2026-05-23 | **7-фазный ритм задачи в CLAUDE.md** | Добавлен раздел `## Task rhythm` в оба CLAUDE.md: 6 фаз — Read → Scope → Skills → Write → Verify (`tsc --noEmit`) → Report. |
| [x] done 2026-05-23 | **audit-then-edit контракт в CLAUDE.md** | Добавлен раздел `## Audit-then-edit` в оба CLAUDE.md: read all → propose list → wait confirmation → apply → report. |
| [x] done 2026-05-23 | **Skill: explore-before-write** | `skills/explore-before-write/SKILL.md` создан в `tests/` и `clinic-mobile-tests/`. **Web:** DOM exploration через Playwright CLI + codegen, curl для API shape, grep для existing clients/steps. **Mobile:** `adb shell uiautomator dump` + grep по resource-id, grep existing steps перед добавлением. Добавлен в Skills index обоих CLAUDE.md. |
| [x] done 2026-05-23 | **enums/{area}/ — единое место для endpoints и сообщений** | **Web (`tests/`):** полная структура — `enums/auth/`, `enums/appointments/`, `enums/doctors/`, `enums/ai/`, `enums/consultations/` — каждый файл содержит `*Endpoints` (пути) + `*Errors` (коды ошибок) + `as const`. `enums/index.ts` — barrel. `data/testData.ts` обновлён: сохранена backward compatibility (`endpoints` объект) + добавлены re-export из enums/ для новых тестов. **Mobile (`clinic-mobile-tests/`):** только `*Errors` (3 файла: auth, appointments, ai) — endpoint пути не нужны, ApiClient их абстрагирует. |
| [x] done 2026-05-23 | **Расширить skills до 15: test-standards, data-strategy, enums, config, helpers, refactor-values** | 6 новых `SKILL.md` созданы в `tests/skills/`: `test-standards` — naming/tags/одна assertion; `data-strategy` — seed accounts vs fixtures vs Faker; `enums` — когда и как использовать enums/; `config` — env vars, baseUrl, known flags; `helpers` — utils/ vs fixtures/ граница; `refactor-values` — безопасный рефакторинг без потери покрытия. Итого: 15 скилов в `tests/` (9 старых + 6 новых). CLAUDE.md Skills index обновлён. |
| [x] done 2026-05-23 | **Субагентная архитектура** | `skills/subagent-workflow/SKILL.md` создан в `tests/` и `clinic-mobile-tests/`. Три роли: Explorer (read-only) / Writer (edit+write) / Reviewer (read-only). Workflow паттерн: Explorer → Main → Writer → Report. Prompt структура для субагента. Параллельные субагенты. Когда НЕ использовать. **Mobile-специфика:** параллельные Android + iOS page objects как пример. Добавлен в Skills index обоих CLAUDE.md.
| [x] done 2026-05-23 | **Dev Container** | `.devcontainer/devcontainer.json` создан в `tests/`. Образ: `mcr.microsoft.com/playwright:v1.49.1-noble` (Node + все браузеры). `postCreateCommand: npm install`. VS Code extensions: Playwright, ESLint, Prettier. `BASE_URL=http://host.docker.internal:3000` — SUT на хосте доступен из контейнера. Студент: `Dev Containers: Reopen in Container` → готово. |
| [~] N/A | **z.strictObject() в схемах** | Проект использует AJV (JSON Schema), а не Zod — `z.object()` не существует в кодовой базе. Эквивалент в AJV: `additionalProperties: false`. Текущие схемы намеренно используют `additionalProperties: true` — API возвращает больше полей чем задокументировано в схемах. Менять на `false` без расширения схем до полного API response shape сломает тесты. Задача требует отдельного аудита ответов API — вынесена в pending-наблюдение. |

---

## QA process artifacts

| Status | Artifact | Location |
|--------|----------|----------|
| [x] | Test strategy | `docs/TEST_STRATEGY.md` |
| [x] | Risk analysis + heatmap | `docs/RISK_ANALYSIS.md` |
| [x] | System weakness report | `docs/SYSTEM_WEAKNESS_REPORT.md` |
| [x] | Portfolio narrative + interview Q&A | `docs/PORTFOLIO_NARRATIVE.md` |
| [x] | Acceptance criteria | `docs/ACCEPTANCE_CRITERIA.md` |
| [x] | Business rules | `docs/BUSINESS_RULES.md` |
| [x] | Known issues register | `docs/KNOWN_ISSUES.md` |
| [x] | Non-technical test summary | `docs/TEST_SUMMARY_STAKEHOLDER.md` |
| [x] | Go / No-Go recommendation | `docs/GO_NO_GO.md` |
| [x] | RTM | `docs/RTM.md` |
| [x] | AI test generation artifact | `docs/AI_TEST_GENERATION.md` |
| [x] | AI gap analysis | `docs/AI_GAP_ANALYSIS.md` |
| [~] | Formal bug reports | skipped 2026-05-16 — KNOWN_ISSUES.md + AI bug reporter sufficient |

---

## DevOps / SRE perspective

| Status | Item |
|--------|------|
| [x] done 2026-05-09 | Dockerfile + docker-compose.test.yml — local == CI |
| [x] done 2026-05-16 | **Global teardown on crash** — `global-teardown.ts`; удаляет всех `test_%@example.com` + payments, consultations, appointments, waitlist; освобождает слоты в pending/confirmed; зарегистрирован в `playwright.config.ts` |

---

## Mobile — Phase 2

Phase 1 (Playwright mobile viewport) — done 2026-05-01. `mobile-chrome` project in this repo runs all `tests/ui/**` on Pixel 7 viewport.

Phase 2 (native app + Appium suite) — full backlog in [`../clinic-mobile/BACKLOG.md`](https://github.com/Ariless/clinic-mobile/blob/main/BACKLOG.md).
Two repos: `clinic-mobile` (React Native SUT) + `clinic-mobile-tests` (Appium + WebdriverIO + Cucumber).

---

## Skipped / decided against

| Item | Reason | Date |
|------|--------|------|
| Audit trail DB table | Pino + Loki covers at production level | — |
| Redis caching | No new QA test patterns for portfolio | 2026-05-16 |
| Admin dashboard | No meaningful test scenarios | 2026-05-16 |
| Formal bug reports | KNOWN_ISSUES.md + AI bug reporter sufficient | 2026-05-16 |
| ISTQB | Weaker signal vs live project; expensive | 2026-05-16 |
| Elevator pitch document | Conversational skill, not a portfolio artifact | 2026-05-16 |
| Multi-tenancy | Scope too large for demo project; DB schema migration on all entities | 2026-05-16 |
| API versioning | No QA value standalone; only interesting after AI service extraction + real Pact | 2026-05-16 |
