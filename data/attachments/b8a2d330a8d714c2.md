# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ui/visual.test.js >> Visual regression @visual @ui >> register patient page — empty state @visual
- Location: tests/ui/visual.test.js:31:5

# Error details

```
Error: A snapshot doesn't exist at /home/runner/work/clinic-booking-api-tests/clinic-booking-api-tests/tests/ui/visual.test.js-snapshots/register-patient-empty-mobile-chrome-linux.png, writing actual.
```

# Page snapshot

```yaml
- generic [ref=e2]: "{\"errorCode\":\"NOT_FOUND\",\"message\":\"Not found\",\"requestId\":\"60c847a3-a9cc-438e-af64-4107fdd6ccb2\"}"
```

# Test source

```ts
  1  | const { test, expect } = require("@playwright/test");
  2  | 
  3  | // Visual regression — compares page screenshots against committed baselines.
  4  | // To update baselines: npm run test:visual:update
  5  | // Baselines are platform-specific (darwin / linux) — CI baselines are generated separately.
  6  | 
  7  | test.describe("Visual regression @visual @ui", () => {
  8  |     test.use({ colorScheme: "light" });
  9  | 
  10 |     test("login page — empty state @visual", async ({ page }) => {
  11 |         await page.goto("/login");
  12 |         await page.waitForLoadState("networkidle");
  13 |         await expect(page).toHaveScreenshot("login-empty.png", {
  14 |             maxDiffPixelRatio: 0.02,
  15 |             animations: "disabled",
  16 |         });
  17 |     });
  18 | 
  19 |     test("login page — error state after invalid credentials @visual", async ({ page }) => {
  20 |         await page.goto("/login");
  21 |         await page.getByTestId("login-email").fill("wrong@test.com");
  22 |         await page.getByTestId("login-password").fill("wrongpassword");
  23 |         await page.getByTestId("login-submit").click();
  24 |         await expect(page.getByTestId("login-error")).toBeVisible();
  25 |         await expect(page).toHaveScreenshot("login-error.png", {
  26 |             maxDiffPixelRatio: 0.02,
  27 |             animations: "disabled",
  28 |         });
  29 |     });
  30 | 
  31 |     test("register patient page — empty state @visual", async ({ page }) => {
  32 |         await page.goto("/register-patient");
  33 |         await page.waitForLoadState("networkidle");
> 34 |         await expect(page).toHaveScreenshot("register-patient-empty.png", {
     |         ^ Error: A snapshot doesn't exist at /home/runner/work/clinic-booking-api-tests/clinic-booking-api-tests/tests/ui/visual.test.js-snapshots/register-patient-empty-mobile-chrome-linux.png, writing actual.
  35 |             maxDiffPixelRatio: 0.02,
  36 |             animations: "disabled",
  37 |         });
  38 |     });
  39 | });
  40 | 
```