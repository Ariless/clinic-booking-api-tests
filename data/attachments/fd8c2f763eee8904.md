# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/appointments.invalid-transition.test.js >> PATCH /api/v1/appointments/:id/confirm — 422 INVALID_TRANSITION when already confirmed @api
- Location: tests/api/appointments.invalid-transition.test.js:6:1

# Error details

```
Error: user fixture failed: {"errorCode":"RATE_LIMITED","message":"Too many registration attempts; try again later.","requestId":"6716d934-2334-4090-9b83-963a65ea5452"}
```

# Test source

```ts
  1  | const { test: base } = require("@playwright/test");
  2  | const { generateUser } = require("../utils/userUtils");
  3  | const { UserClient } = require("../api/UserClient");
  4  | 
  5  | const test = base.extend({
  6  |     // Setup: creates a unique user via API before the test.
  7  |     // Teardown: deletes the user via API after the test, even if the test fails.
  8  |     // This keeps each test fully isolated — no shared state between tests.
  9  |     user: async ({ request }, use) => {
  10 |         const userClient = new UserClient(request);
  11 |         const userData = generateUser();
  12 | 
  13 |         const { status, body } = await userClient.registerPatient({
  14 |             email: userData.email,
  15 |             password: userData.password,
  16 |             name: userData.name ?? "Test Patient",
  17 |         });
  18 | 
  19 |         if (status !== 201 || !body.token) {
> 20 |             throw new Error(`user fixture failed: ${JSON.stringify(body)}`);
     |                   ^ Error: user fixture failed: {"errorCode":"RATE_LIMITED","message":"Too many registration attempts; try again later.","requestId":"6716d934-2334-4090-9b83-963a65ea5452"}
  21 |         }
  22 | 
  23 |         const userPayload = {
  24 |             ...userData,
  25 |             token: body.token,
  26 |             refreshToken: body.refreshToken,
  27 |             user: body.user,
  28 |         };
  29 | 
  30 |         await use(userPayload);
  31 | 
  32 |         await userClient.deleteMyAccount(userPayload.token);
  33 |     },
  34 | });
  35 | 
  36 | module.exports = { test, expect: base.expect };
  37 | 
```